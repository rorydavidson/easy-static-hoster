services:
  generator:
    image: rorydavidson/easy-static-hoster-generator:latest
    build: ./generator
    volumes:
      - ${CONTENT_DIR:-./content}:/content
    environment:
      SITE_TITLE: ${SITE_TITLE:-EasyHoster}
      HEADER_COLOR: ${HEADER_COLOR:-}
      BASIC_AUTH: ${BASIC_AUTH:-}
      OIDC_ISSUER_URL: ${OIDC_ISSUER_URL:-}
    expose:
      - "5000"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "test", "-f", "/content/index.html"]
      interval: 2s
      timeout: 5s
      retries: 15
      start_period: 5s

  nginx:
    image: rorydavidson/easy-static-hoster-nginx:latest
    build: ./nginx
    ports:
      - "${PORT:-8080}:8080"
    volumes:
      - ${CONTENT_DIR:-./content}:/content:ro
    environment:
      BASIC_AUTH: ${BASIC_AUTH:-}
      AUTH_GLOBAL: ${AUTH_GLOBAL:-}
      OIDC_ISSUER_URL: ${OIDC_ISSUER_URL:-}
    depends_on:
      generator:
        condition: service_healthy
    restart: unless-stopped
